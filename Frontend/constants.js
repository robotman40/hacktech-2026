/**
 * In-page alert and extension UI: display name of the product.
 * @type {string}
 */
var KANDOR_APP_ALERT_TITLE = "kandor alert";

/**
 * Display / marketing serif — same face as `font-serif` on the Landing (Fraunces + fallbacks).
 * Keep in sync with `Landing/src/styles/theme.css` @theme --font-serif.
 * @type {string}
 */
var KANDOR_DISPLAY_SERIF =
  '"Fraunces", ui-serif, Georgia, Cambria, "Times New Roman", Times, serif';
